import H2 from "./elements/h2";
import P from "./elements/p";
import { Button } from "react-bootstrap";

export default function WizardPagination() {
  //todo: fetch from db
  const menuItems = {
    Projectgegevens: [],
    Categorieën: ['Thema"s', "Lucht", "Klimaat", "Licht", "Geluid", "Kwaliteit"],
    Resultaten: [],
  };

  return <></>;
}
